=>Node.js:-

Node js is a javascript runtime built on crome's v8 javascript engine. Node js is used an event-driven, 
non-blocking I/O modal that it lightweight and efficient. Node js package ecosystem, npm is the largest ecosystem of open source
libraries in the world.


-write txt to the txt file:-
const fs = require('fs');

fs.writeFileSync('notes.txt', 'I love to write code');

by using appendFileSync
fs.appendFileSync('notes.txt', 'I');

=>installation :- install npm.
install npm init for the node project.

